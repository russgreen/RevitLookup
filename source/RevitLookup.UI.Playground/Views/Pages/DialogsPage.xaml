<Page
    x:Class="RevitLookup.UI.Playground.Client.Views.Pages.DialogsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ui="http://lookupengine.com/xaml"
    xmlns:pages="clr-namespace:RevitLookup.UI.Playground.Client.Views.Pages"
    mc:Ignorable="d"
    d:DesignHeight="1500"
    d:DesignWidth="500"
    d:DataContext="{d:DesignInstance pages:DialogsPage}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <StackPanel
        Margin="0 24">
        <ui:TextBlock
            Text="About program"
            FontTypography="BodyStrong" />
        <ui:CardAction
            Margin="0 4 0 0"
            Icon="{ui:SymbolIcon HandOpenHeart32}"
            Content="Open source dialog"
            Command="{Binding ViewModel.ShowOpenSourceDialogCommand}" />

        <ui:TextBlock
            Margin="0 12 0 0"
            Text="Tools"
            FontTypography="BodyStrong" />
        <ui:CardAction
            Margin="0 4 0 0"
            Icon="{ui:SymbolIcon SlideSearch24}"
            Content="Search elements dialog"
            Command="{Binding ViewModel.ShowSearchElementsDialogCommand}" />
        <ui:CardAction
            Margin="0 4 0 0"
            Icon="{ui:SymbolIcon BroadActivityFeed24}"
            Content="Modules dialog"
            Command="{Binding ViewModel.ShowModulesDialogCommand}" />
        <ui:CardAction
            Margin="0 4 0 0"
            Icon="{ui:SymbolIcon LeafOne24}"
            Content="BuiltIn parameters dialog"
            Command="{Binding ViewModel.ShowParametersDialogCommand}" />
        <ui:CardAction
            Margin="0 4 0 0"
            Icon="{ui:SymbolIcon LeafTwo24}"
            Content="BuiltIn categories dialog"
            Command="{Binding ViewModel.ShowCategoriesDialogCommand}" />
        <ui:CardAction
            Margin="0 4 0 0"
            Icon="{ui:SymbolIcon LeafThree24}"
            Content="Forge schema dialog"
            Command="{Binding ViewModel.ShowForgeSchemaDialogCommand}" />

        <ui:TextBlock
            Margin="0 12 0 0"
            Text="Visualization"
            FontTypography="BodyStrong" />
        <ui:CardAction
            Margin="0 4 0 0"
            Icon="{ui:SymbolIcon Wallpaper24}"
            Content="BoundingBox visualization dialog"
            Command="{Binding ViewModel.ShowBoundingBoxVisualizationDialogCommand}" />
        <ui:CardAction
            Margin="0 4 0 0"
            Icon="{ui:SymbolIcon Wallpaper24}"
            Content="Face visualization dialog"
            Command="{Binding ViewModel.ShowFaceVisualizationDialogCommand}" />
        <ui:CardAction
            Margin="0 4 0 0"
            Icon="{ui:SymbolIcon Wallpaper24}"
            Content="Mesh visualization dialog"
            Command="{Binding ViewModel.ShowMeshVisualizationDialogCommand}" />
        <ui:CardAction
            Margin="0 4 0 0"
            Icon="{ui:SymbolIcon Wallpaper24}"
            Content="Polyline visualization dialog"
            Command="{Binding ViewModel.ShowPolylineVisualizationDialogCommand}" />
        <ui:CardAction
            Margin="0 4 0 0"
            Icon="{ui:SymbolIcon Wallpaper24}"
            Content="CurveLoop visualization dialog"
            Command="{Binding ViewModel.ShowCurveLoopVisualizationDialogCommand}" />
        <ui:CardAction
            Margin="0 4 0 0"
            Icon="{ui:SymbolIcon Wallpaper24}"
            Content="Solid visualization dialog"
            Command="{Binding ViewModel.ShowSolidVisualizationDialogCommand}" />
        <ui:CardAction
            Margin="0 4 0 0"
            Icon="{ui:SymbolIcon Wallpaper24}"
            Content="XYZ visualization dialog"
            Command="{Binding ViewModel.ShowXyzVisualizationDialogCommand}" />

        <ui:TextBlock
            Margin="0 12 0 0"
            Text="Edit dialogs"
            FontTypography="BodyStrong" />
        <ui:CardAction
            Margin="0 4 0 0"
            Icon="{ui:SymbolIcon Edit24}"
            Content="Edit value dialog"
            Command="{Binding ViewModel.ShowEditValueDialogCommand}" />
        <ui:CardAction
            Margin="0 4 0 0"
            Icon="{ui:SymbolIcon EditSettings24}"
            Content="Edit INI entry dialog"
            Command="{Binding ViewModel.ShowUpdateIniEntryDialogCommand}" />
        <ui:CardAction
            Margin="0 4 0 0"
            Icon="{ui:SymbolIcon EditSettings24}"
            Content="Create INI entry dialog"
            Command="{Binding ViewModel.ShowCreateIniEntryDialogCommand}" />
    </StackPanel>
</Page>