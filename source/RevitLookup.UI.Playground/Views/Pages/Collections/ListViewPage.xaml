<Page
    x:Class="RevitLookup.UI.Playground.Views.Pages.Collections.ListViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:RevitLookup.UI.Playground.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:RevitLookup.UI.Playground.Views.Pages.Collections"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:RevitLookup.UI.Playground.Models"
    xmlns:ui="http://lookupengine.com/xaml"
    d:DataContext="{d:DesignInstance local:ListViewPage}"
    d:DesignHeight="750"
    d:DesignWidth="800"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <StackPanel Margin="0,0,0,24">
        <controls:ControlExample
            Margin="0"
            HeaderText="Basic ListView with Simple DataTemplate"
            XamlCode="&lt;ui:ListView ItemsSource=&quot;{Binding ViewModel.Persons}&quot;&gt;&#x0a;&#x09;&lt;ui:ListView.ItemTemplate&gt;&#x0a;&#x09;&#x09;&lt;DataTemplate DataType=&quot;{x:Type models:Person}&quot;&gt;&#x0a;&#x09;&#x09;&#x09;&lt;TextBlock Margin=&quot;8,4&quot; Text=&quot;{Binding Name}&quot; /&gt;&#x0a;&#x09;&#x09;&lt;/DataTemplate&gt;&#x0a;&#x09;&lt;/ui:ListView.ItemTemplate&gt;&#x0a;&lt;/ui:ListView&gt;">
            <ui:ListView
                MaxHeight="200"
                ItemsSource="{Binding ViewModel.Persons, Mode=TwoWay}"
                SelectedIndex="2"
                SelectionMode="Single">
                <ui:ListView.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:Person}">
                        <TextBlock Margin="8,4" Text="{Binding Name, Mode=OneWay}" />
                    </DataTemplate>
                </ui:ListView.ItemTemplate>
            </ui:ListView>
        </controls:ControlExample>

        <controls:ControlExample
            Margin="0,32,0,0"
            HeaderText="ListView with Selection Support"
            XamlCode="&lt;ui:ListView ItemsSource=&quot;{Binding ViewModel.Persons}&quot; SelectionMode=&quot;{Binding ViewModel.ListViewSelectionMode}&quot;&gt;&#x0a;&#x09;&lt;ui:ListView.ItemTemplate&gt;&#x0a;&#x09;&#x09;&lt;DataTemplate DataType=&quot;{x:Type models:Person}&quot;&gt;&#x0a;&#x09;&#x09;&#x09;&lt;Grid Margin=&quot;8,0&quot;&gt;&#x0a;&#x09;&#x09;&#x09;&#x09;&lt;TextBlock Text=&quot;{Binding Name}&quot; /&gt;&#x0a;&#x09;&#x09;&#x09;&lt;/Grid&gt;&#x0a;&#x09;&#x09;&lt;/DataTemplate&gt;&#x0a;&#x09;&lt;/ui:ListView.ItemTemplate&gt;&#x0a;&lt;/ui:ListView&gt;">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ui:ListView
                    Grid.Column="0"
                    MaxHeight="200"
                    d:ItemsSource="{d:SampleData ItemCount=2}"
                    ItemsSource="{Binding ViewModel.Persons, Mode=TwoWay}"
                    SelectedIndex="1"
                    SelectionMode="{Binding ViewModel.SelectionMode, Mode=OneWay}">
                    <ui:ListView.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:Person}">
                            <Grid Margin="8,0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Ellipse
                                    x:Name="Ellipse"
                                    Grid.RowSpan="2"
                                    Width="32"
                                    Height="32"
                                    Margin="6"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Fill="{ui:ThemeResource ControlStrongStrokeColorDefaultBrush}" />
                                <TextBlock
                                    Grid.Row="0"
                                    Grid.Column="1"
                                    Margin="12,6,0,0"
                                    FontWeight="Bold"
                                    Text="{Binding Name, Mode=OneWay}" />
                                <TextBlock
                                    Grid.Row="1"
                                    Grid.Column="1"
                                    Margin="12,0,0,6"
                                    Text="{Binding Company, Mode=OneWay}" />
                            </Grid>
                        </DataTemplate>
                    </ui:ListView.ItemTemplate>
                </ui:ListView>
                <StackPanel
                    Grid.Column="1"
                    MinWidth="120"
                    Margin="12,0,0,0"
                    VerticalAlignment="Top">
                    <Label Content="Selection mode" Target="{Binding ElementName=SelectionModeComboBox}" />
                    <ComboBox x:Name="SelectionModeComboBox" SelectedIndex="{Binding ViewModel.SelectionModeIndex, Mode=TwoWay}">
                        <ComboBoxItem Content="Single" />
                        <ComboBoxItem Content="Multiple" />
                        <ComboBoxItem Content="Extended" />
                    </ComboBox>
                </StackPanel>
            </Grid>
        </controls:ControlExample>

        <controls:ControlExample
            Margin="0,32,0,0"
            HeaderText="ListView with GridView"
            XamlCode="&lt;ui:ListView ItemsSource=&quot;{Binding ViewModel.Persons}&quot;&gt;&#x0a;&#x09;&lt;ui:ListView.View&gt;&#x0a;&#x09;&#x09;&lt;ui:GridView&gt;&#x0a;&#x09;&#x09;&#x09;&lt;ui:GridViewColumn DisplayMemberBinding=&quot;{Binding FirstName}&quot; Header=&quot;First Name&quot; MinWidth=&quot;100&quot; /&gt;&#x0a;&#x09;&#x09;&#x09;&lt;ui:GridViewColumn DisplayMemberBinding=&quot;{Binding LastName}&quot; Header=&quot;Last Name&quot; MinWidth=&quot;100&quot; /&gt;&#x0a;&#x09;&#x09;&#x09;&lt;ui:GridViewColumn DisplayMemberBinding=&quot;{Binding Company}&quot; Header=&quot;Company&quot; MinWidth=&quot;100&quot; /&gt;&#x0a;&#x09;&#x09;&lt;/ui:GridView&gt;&#x0a;&#x09;&lt;/ui:ListView.View&gt;&#x0a;&lt;/ui:ListView&gt;">
            <ui:ListView
                MaxHeight="200"
                d:ItemsSource="{d:SampleData ItemCount=2}"
                BorderThickness="0"
                ItemsSource="{Binding ViewModel.Persons, Mode=TwoWay}">
                <ui:ListView.View>
                    <ui:GridView>
                        <ui:GridViewColumn
                            MinWidth="100"
                            DisplayMemberBinding="{Binding FirstName}"
                            Header="First Name" />
                        <ui:GridViewColumn
                            MinWidth="100"
                            DisplayMemberBinding="{Binding LastName}"
                            Header="Last Name" />
                        <ui:GridViewColumn
                            MinWidth="100"
                            DisplayMemberBinding="{Binding Company}"
                            Header="Company" />
                    </ui:GridView>
                </ui:ListView.View>
            </ui:ListView>
        </controls:ControlExample>
    </StackPanel>
</Page>